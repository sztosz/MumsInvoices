nav.navbar.navbar-default(role="navigation")
  .container-fluid
    .navbar-header
      .button.navbar-toggle.collapsed(type="button", data-toggle="collapse", data-target="#mums-invoices-nav")
        span.sr-only Zwiń Menu
        span.icon-bar
        span.icon-bar
        span.icon-bar
      a.navbar-brand(href="/") Faktury Mamy

    .collapse.navbar-collapse#mums-invoices-nav
      ul.nav.navbar-nav.pull-right
        if user.is_authenticated
          li
            a(href="/+{{ user.username }}")= user.username
          li
            a(href="/+{{ user.username }}/settings")= Ustawienia
          li
            a(href="javascript:void(0)")= Wyloguj
        else
          li
            a(href="/login") Login
          li
            a(href="/register") Rejestracja
